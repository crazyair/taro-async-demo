import { View } from "@tarojs/components";

const Demo = () => {
  return (
    <View
      onClick={async () => {
        console.log("111");
      }}
    >
      demo
    </View>
  );
};

export default Demo;
